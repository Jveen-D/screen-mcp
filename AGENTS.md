# Screen MCP Agent Rules

这些规则适用于所有 AI/Agent 对本项目的修改。修改代码、文档、测试或示例前，先阅读并遵守本文件。

## 项目目标

`screen-component-mcp` 是大屏编辑器 schema 的能力编译器，不是大屏模板生成器。

核心边界：

- LLM 负责根据用户描述自主决定主题、颜色、模块、布局、文案、装饰和组件组合。
- MCP 负责暴露组件/模块能力、校验结构、合并默认 props、规范输出完整编辑器 schema。
- 任何新增能力都应增强 LLM 的自由表达、质量检查或 schema 编译能力，不能把设计决策重新写死在 MCP 内部。

## 禁止事项

- 不要新增行业模板、主题模板、布局模板或 prompt 到模板的映射。
- 不要让相同描述固定复用某个历史模板、缓存模板或预置视觉方案。
- 不要在 MCP 中根据关键词硬编码颜色、模块数量、模块位置、图表类型或装饰风格。
- 不要把 `ChartPanel` 扩展成万能模块。它只代表图表分析面板。
- 不要用自动生成的固定装饰、固定标题栏、固定背景去覆盖 LLM 显式传入的设计。
- 不要让 LLM 猜测或选择项目里的现有素材路径。现有素材是大屏生成后给用户替换元素用的；`imageSrc` 只能使用用户明确提供的路径。
- 不要让 `SvgDecoration` 退回固定 preset 图标作为默认装饰；AI 需要装饰时应生成安全的 custom `svgContent`。
- 不要让背景图层遮挡主内容。模块和全屏 `SingleImage` 背景必须保持底层 zIndex，并在分组中位于背景组。
- 不要为了通过单个 demo 而加入面向某个业务场景的特殊逻辑。

## 推荐方向

- 优先改能力说明、输入 schema、校验、规范化、编译和测试。
- 需要支持非图表或混合模块时，优先使用或增强 `FreeformModule`。
- 需要支持模块内结构时，优先使用通用分组能力，而不是写到单个模块里。
- 需要提升生成质量时，优先增加 validator warning/error，例如越界、重叠、背景遮挡、文本高度不足、Gauge 重复数值、Indicator 宽度不足等。
- 保持 DashboardSpec 流程：LLM 先设计完整 spec，再调用 MCP 校验和编译。

## 模块约定

- `ChartPanel`：图表分析面板。主内容由 `slots.mainChart` 承载，适合饼图、柱状图、折线图、玫瑰图、散点图等图表分析。
- `FreeformModule`：自由模块。通过 `slots.children` 接收 LLM 明确提供的任意组件，适合 KPI、表格、地图、媒体、控制器和混合信息卡。
- 通用语义分组应面向所有模块，而不是 `ChartPanel` 专属。
- 分组后的层级顺序必须服务渲染：数组越靠前越在顶层；主内容应位于装饰和背景之上，背景组必须最后。
- 每个模块都应有背景承载。LLM 应主动设计同一大屏内风格一致的模块背景；MCP 可以在缺省时补同主题的轻量背景，但不能替换 LLM 显式提供的背景。
- 同一大屏内各模块的背景、边框、角标和结构线应共享设计语言；不要把一致性写成固定模板或行业样式。
- 如果用户要求模块内部也分组，应支持：

```json
{
  "grouping": {
    "mode": "semantic",
    "singleChildGroup": true
  }
}
```

DashboardSpec 顶层的 `grouping` 应能被未单独设置 `grouping` 的模块继承。

## 修改流程

1. 先读相关现有实现和 README，不凭记忆改。
2. 使用 `rg` 查找已有能力、类型、测试和旧逻辑。
3. 修改前确认改动是否会引入模板化、关键词分流或固定设计决策。
4. 优先做小范围、可验证的改动，避免顺手重构无关文件。
5. 修改后根据影响范围运行验证。常用命令：

```bash
npm run build
npm run test:flow
```

## 输出与文档

- 面向用户或 README 的说明要反复强调：LLM 负责设计，MCP 负责编译和校验。
- 示例可以展示结构写法，但不要把示例描述成推荐唯一布局或默认模板。
- 新增组件/模块时必须补能力说明，让 LLM 明白何时使用、如何传入最小 props、有哪些约束。
